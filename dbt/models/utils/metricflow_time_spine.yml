version: 2

models:
  - name: metricflow_time_spine
    description: "Time spine for MetricFlow Semantic Layer — one row per day, 2017-2025."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
