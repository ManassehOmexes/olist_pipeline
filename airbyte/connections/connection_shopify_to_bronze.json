{
  "name": "shopify-${CUSTOMER_ID}-to-bronze",
  "syncCatalog": {
    "streams": [
      {
        "stream": { "name": "orders" },
        "config": { "syncMode": "incremental", "destinationSyncMode": "append_dedup" }
      },
      {
        "stream": { "name": "products" },
        "config": { "syncMode": "incremental", "destinationSyncMode": "append_dedup" }
      },
      {
        "stream": { "name": "customers" },
        "config": { "syncMode": "incremental", "destinationSyncMode": "append_dedup" }
      },
      {
        "stream": { "name": "order_refunds" },
        "config": { "syncMode": "incremental", "destinationSyncMode": "append_dedup" }
      }
    ]
  },
  "scheduleType": "cron",
  "scheduleData": { "cron": { "cronExpression": "0 5 * * *", "cronTimeZone": "Europe/Berlin" } },
  "status": "active"
}
